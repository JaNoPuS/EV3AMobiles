<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceso a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login4"
        tools:targetApi="31">

        <!-- Declaración de MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de ActivityWelcome -->
        <activity
            android:name=".ActivityWelcome"
            android:label="Bienvenido"
            android:exported="true" />

        <!-- Declaración de CreateTripActivity -->
        <activity
            android:name=".CreateTripActivity"
            android:label="Nuevo Viaje"
            android:exported="true" />

        <!-- Declaración de ExperiencesHistoryActivity -->
        <activity
            android:name=".ExperiencesHistoryActivity"
            android:label="Historial de Experiencias"
            android:exported="true" />

        <!-- Declaración de FriendsActivity -->
        <activity
            android:name=".FriendsActivity"
            android:label="Mis Amigos"
            android:exported="true" />

        <!-- Declaración de ProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:label="Mi Perfil"
            android:exported="true" />

        <!-- Declaración de ExperienceDetailActivity -->
        <activity
            android:name=".ExperienceDetailActivity"
            android:label="Detalles de la Experiencia"
            android:exported="true" />

    </application>
</manifest>
